{
  "name": "rg-notebook",
  "version": "1.0.0",
  "description": "Employee used this application to memorise during work. anything which need to not easily can save here.",
  "main": "index.html",
  "scripts": {
    "start": "nodemon server",
    "clean": "node scripts/build.js --clean",
    "build": "node scripts/build.js",
    "start:dist": "node dist/server.js",
    "serve:local": "npm run clean && npm run build && npm run start:dist"
  },
  "author": "rajib.ganguly@ltimindtree.com",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^2.2.0",
    "express": "^5.1.0",
    "nodemon": "^3.1.10"
  }
}
